name: prepare_data

conda_env: conda.yml

entry_points:
  main:
    parameters:
      input_path: string
      output_path: string
    command: "python run.py --input_path {input_path} --output_path {output_path}"
